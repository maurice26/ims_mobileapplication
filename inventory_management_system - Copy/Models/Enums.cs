namespace inventory_management_system.Models {
    public enum UserRole
    {
        Admin,
        Warehouse,
        Sales,
        Accountant
    }

    public enum PaymentStatus
    {
        Pending,
        Completed,
        Failed
    }
}
