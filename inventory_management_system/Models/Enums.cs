namespace inventory_management_system.Models {
    public enum UserRole
    {
        Admin,
        Sales,
        user 
    }

    public enum PaymentStatus
    {
        Pending,
        Completed,
        Failed
    }
}
